# This is a Harness primitive.
# See also: library://meta-harness

name = "proj-quartz"
description = "Quartz project knowledge and application for using Meta Harness Knowledge Agent with governed Libraries."
isSystemLibrary = true
read_actors = ["actor://proj-quartz/agent"]
update_actors = []
agent_excludes = [
  "app/node_modules/**",
  "app/vendor/**",
  "app/.next/**",
]
