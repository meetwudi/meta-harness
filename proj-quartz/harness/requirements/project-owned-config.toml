id = "proj-quartz.project-owned-config"
title = "Project-owned configuration"
source = "Human request on 2026-06-26: Quartz should have its own configuration file for Quartz-scoped runtime behavior."
text = "Quartz uses a project-owned `.meta-harness.json` runtime configuration so its Knowledge Agent view is scoped to Quartz and its configured backing storage."
