id = "proj-quartz.acceptance.knowledge-agent-chat-service"
title = "Chat service reaches Knowledge Agent"
requirements = [
  "proj-quartz.knowledge-agent-chat-service",
]
procedure = [
  "Start the PROJ-Quartz application.",
  "Open the main page.",
  "Send a message through the chat service while the Knowledge Agent provider environment is configured.",
]
expected = [
  "The main page presents the chat service as the primary working surface.",
  "The chat service sends the user message to the local Knowledge Agent endpoint.",
  "The Knowledge Agent response renders back in the chat service.",
]
