# Quartz Deployment Runs

Source: human request on 2026-07-01 for deployment artifacts, logs, history, and checklist attestation for each deployment.

Each deployment run should produce a run record and artifact references in this folder or in another human-approved storage location. The policy for checking in raw provider logs is not decided yet. Until that policy is explicit, consult the human before adding raw logs, cloud command output, or files that may contain secret values.

Use `run-template.toml` for each deployment run record.
