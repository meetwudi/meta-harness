id = "proj-quartz.integration.secret-toolspec-storage"
title = "Quartz Secret ToolSpec storage and redaction"
requirements = [
  "proj-quartz.secret-primitive-onboarding",
  "proj-quartz.secret-scope",
  "proj-quartz.secret-use-reveal-policy",
  "proj-quartz.secret-encrypted-library-storage",
]
procedure = [
  "Build the Meta Harness Knowledge Agent and Librarian implementation.",
  "Discover the Quartz `quartz_secret` ToolSpec from `library://proj-quartz` under the active Quartz actor set.",
  "Verify the generic Knowledge Agent ToolSpec adapter exposes `quartz_secret` as an agent tool and preserves its ordinary-language create/reveal guidance.",
  "Run `node proj-quartz/toolspecs/quartz-secret/tests/integration.test.mjs` to execute the temporary-Postgres ToolSpec integration test.",
  "Invoke the ToolSpec in a test runtime with an explicit `QUARTZ_USER_SECRET_ENCRYPTION_KEY` and Postgres-backed storage.",
  "Store a personal Secret corresponding to ordinary user wording: `Here is my Acme API key: qz_acceptance_secret_12345. Please remember it as my Acme API key.`",
  "Store an organization-scoped Secret under the same active organization tenant.",
  "List Secrets and verify only metadata is returned.",
  "Reveal the Secret with an authorized user actor corresponding to ordinary user wording: `What is my Acme API key?`",
  "Inspect the Secret Library resources and persisted session records.",
]
expected = [
  "The ToolSpec is discovered through the generic Library ToolSpec path.",
  "The ToolSpec is exposed as an executable Knowledge Agent tool through the generic ToolSpec adapter, without runtime hard-coding of the concrete Secret tool.",
  "The stored personal Secret Library is under `/libraries/organizations/{organization_id}/users/{user_id}/secrets/{secret_library_name}` and contains `SECRET.toml`, encrypted `SECRET-VALUE.json`, and `AUDIT.jsonl`.",
  "The stored organization Secret Library is under `/libraries/organizations/{organization_id}/secrets/{secret_library_name}`.",
  "No Secret Library is stored directly under `/libraries`.",
  "`SECRET-VALUE.json` is omitted from ordinary Librarian file listings and direct Librarian reads fail with an agent-excluded path error.",
  "The plaintext Secret value is absent from Secret metadata, audit records, Librarian traces, prompt history, and persisted session JSONL.",
  "Reveal returns plaintext only through the authorized reveal operation.",
]
