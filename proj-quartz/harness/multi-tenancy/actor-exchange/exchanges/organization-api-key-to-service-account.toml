id = "proj-quartz.actor-exchange.organization-api-key-to-service-account"
title = "Organization API key to service account actor"
source = "Human clarification on 2026-07-02: organizational API keys should have their own actor, such as a service account actor."
requirements = [
  "proj-quartz.multi-tenancy.actor-acquisition",
  "proj-quartz.multi-tenancy.api-key-tenant-scope",
  "proj-quartz.multi-tenancy.organization-api-key-service-account-actors",
]
input_actors = [
  "The actor URI recorded on the authenticated organization-scoped API key.",
]
requested_actor = "organization-service-account-authority-for-api-key"
allow = [
  "The API key is valid, not revoked, and records an organization tenant.",
  "The API key was created as an organization-scoped key by an actor authorized to create organization service account authority.",
  "The requested operation is within the API key's recorded organization tenant and service account scope.",
]
deny = [
  "The API key is missing, revoked, or invalid.",
  "The API key does not record an organization tenant.",
  "The API key was not created through an organization-scoped service account creation path.",
  "The requested operation is outside the API key's recorded organization tenant or service account scope.",
]
output_actors = [
  "The organization API key's service-account-style actor: `actor://proj-quartz/organization/{organizationId}/service-account/{apiKeyId}`.",
  "The API key's recorded organization tenant context.",
]
