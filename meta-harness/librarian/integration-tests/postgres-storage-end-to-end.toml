id = "librarian.integration.postgres-storage-end-to-end"
title = "Postgres storage Library operations"
requirements = [
  "storage.postgres-driver",
  "storage.postgres-schema-bootstrap",
  "storage.postgres-deployment-configuration",
  "librarian.postgres-backed-library-interface",
  "librarian.library-delete-governance",
  "librarian.tool-librarian-delete",
]
procedure = [
  "Start an isolated local Postgres instance.",
  "Create a Librarian context with a Postgres-backed storage location and explicit table configuration.",
  "Create a read-only Library fixture in the same Postgres-backed storage location.",
  "Create a Library through the Librarian create-Library tool.",
  "List Libraries through the Librarian list-Libraries tool.",
  "Write a Library resource through the Librarian update tool.",
  "Read the Library resource through the Librarian read tool.",
  "List Library files through the Librarian list-files tool.",
  "Query Library content through the Librarian search tool.",
  "Write nested folder resources through the Librarian update tool.",
  "Delete the nested folder resource through the Librarian delete tool.",
  "List Library files and verify the deleted folder descendants are gone.",
  "Add Tags to a Library scope and query by those Tags.",
  "Attempt to delete a read-only Library visible in the same storage location.",
  "Attempt to delete a resource inside a read-only Library visible in the same storage location.",
  "Delete a Library that is writable to the active actor.",
]
expected = [
  "The Postgres driver bootstraps its schema with raw SQL.",
  "The configured Postgres table is used for Library resources.",
  "The created Library is returned by Library listing.",
  "The created Library includes the required description and non-system metadata.",
  "The written resource can be read back by Library URI.",
  "The written resource appears in Library file listing.",
  "The deleted nested folder resource and descendants disappear from Library file listing.",
  "The written resource is returned by Library search.",
  "The tagged scope is returned by Tag query.",
  "Deleting a read-only Library is rejected.",
  "Deleting a resource from a read-only Library is rejected.",
  "Deleting a writable Library removes it from Library listing.",
]
