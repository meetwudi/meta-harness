# Knowledge Agent

This folder specifies the Meta Harness knowledge agent.

Work here is spec-driven. Update [SPEC.toml](SPEC.toml), requirements, and
acceptance tests before implementation code whenever behavior, interfaces,
provider choices, prompts, or runtime rules change.

Start with [SPEC.toml](SPEC.toml), then read the referenced requirement and
acceptance-test collections.
