id = "knowledge-agent.integration.postgres-storage-config"
title = "Postgres storage location configuration"
requirements = [
  "knowledge-agent.local-filesystem-storage-compatibility",
  "knowledge-agent.project-config-selection",
  "storage.env-gated-storage-locations",
  "storage.project-scoped-storage-locations",
  "storage.postgres-deployment-configuration",
  "proj-quartz.project-owned-config",
  "proj-quartz.postgres-backed-libraries",
  "proj-quartz.quartz-agent-actor",
  "proj-quartz.default-library-creation-target",
  "proj-quartz.memory-curator-actor",
  "proj-quartz.stock-memory-setup-natural-language",
  "knowledge-agent.memory-curation-config",
  "knowledge-agent.library-scoped-memory-curator",
]
procedure = [
  "Create repository self storage knowledge with a machine-local filesystem location.",
  "Create project storage knowledge with project-scoped filesystem locations and an env-gated Postgres location.",
  "Mark the Postgres-backed storage location as the default Library creation target.",
  "Materialize storage locations from the project config without the Postgres environment variable.",
  "Start an isolated local Postgres instance and set the Postgres environment variable.",
  "Materialize storage locations from the project config again.",
  "Create a Library without naming a storage location, then write and read it through the configured Postgres-backed storage location.",
  "Create a stocks Library from ordinary user-language inputs without naming or exposing the storage location.",
  "Write `library://stocks/MEMORY.toml` through Librarian with automatic curation, one folder per stock symbol, and sequential dated entry guidance derived from the user-language request.",
  "Write an example FB entry under the per-stock folder using a dated sequential file.",
  "Materialize storage locations for the configured Quartz Memory Curator actor and verify it can inspect the stocks Library and Memory config.",
  "Render Memory Curator mode with a contextualized main-agent goal and a separate raw latest user message.",
]
expected = [
  "Project filesystem locations remain available when the Postgres environment variable is absent.",
  "The repository self machine-local location is not inherited by the selected project config.",
  "The Postgres location is skipped when the deployment environment variable is absent.",
  "The Postgres location is materialized when the deployment environment variable is present.",
  "The configured Postgres-backed location can create, write, and read Libraries through the Librarian interface.",
  "The configured default creation target is used when Library creation omits a storage location name.",
  "Project storage grants are evaluated against the project-configured Quartz actor.",
  "New Quartz-created Libraries grant read and update governance to the Quartz Memory Curator actor.",
  "`MEMORY.toml` curation config can mark the stock Memory collection as automatically curated.",
  "`MEMORY.toml` specifies the stock memory layout as `symbols/{symbol}/` folders with `YYYY-MM-DD.md` sequential entries and learned-at/learned-from provenance.",
  "The Memory Curator actor sees the stocks Library as writable and can read its `LIBRARY.toml` and `MEMORY.toml` before writing.",
  "Memory Curator mode uses a separate latest-user-message boundary from the contextualized main-agent goal.",
]
