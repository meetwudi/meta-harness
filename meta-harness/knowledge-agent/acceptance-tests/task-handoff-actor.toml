id = "knowledge-agent.acceptance.task-handoff-actor"
title = "Task handoff actor"
requirements = [
  "knowledge-agent.task-handoffs",
  "knowledge-agent.uses-librarian",
  "knowledge-agent.library-scoped-access",
  "knowledge-agent.prompt-calls-librarian-intro",
]
procedure = [
  "Start Knowledge Agent with a repository that contains at least one Meta Harness Task.",
  "Ask the Knowledge Agent to run that Task.",
  "Inspect the task handoff agent's Librarian context and conversation trace.",
]
expected = [
  "The Knowledge Agent exposes the matching Task as an OpenAI Agents SDK handoff.",
  "The handoff agent receives the shared Knowledge Agent starter prompt rendered in Task mode with task selection metadata.",
  "The handoff input includes the Task-mode starter prompt for the selected task.",
  "The Task-mode prompt points the agent to the Task primitive and source Library instead of duplicating the Task execution procedure.",
  "The handoff agent uses Librarian tools with both the base Knowledge Agent actor and the Task actor identity active.",
  "The Task actor identity follows the source Library and task identity.",
  "The handoff agent's recorded Librarian tool calls begin with librarian_intro before other Library operations.",
  "The handoff agent updates Library resources through Librarian instead of sandbox filesystem patch tools.",
  "Librarian tool calls from the handoff agent are recorded in the conversation trace.",
  "The task handoff produces a non-empty final response with completion evidence or a blocker.",
]
