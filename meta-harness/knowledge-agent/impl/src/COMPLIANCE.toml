# Harness-Compliance: Review applicable compliance with an independent subagent before attesting; if subagent use requires human approval, ask for approval.
# This is a Harness primitive.
# See also: library://meta-harness

[[items]]
id = "knowledge-agent-toolspec-generic-resolution"
text = "Verify that Knowledge Agent ToolSpec OpenAI adapter code remains generic: discovered ToolSpecs may be exposed after Library discovery and actor governance, but the adapter must not branch on specific tool names, implementation tokens, or implementation file names."
