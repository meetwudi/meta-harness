# Memory Curation Routine

Read [ROUTINE.toml](ROUTINE.toml) before changing Memory Curator behavior.

Keep curation behavior Library-scoped. Domain-specific layouts, identifier
handling, duplicate behavior, and write policy belong in each routed Library's
`MEMORY.toml`, not in this Routine or the shared Knowledge Agent prompt.
