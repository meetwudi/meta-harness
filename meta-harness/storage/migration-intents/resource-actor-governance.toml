id = "storage.migration-intent.resource-actor-governance"
title = "Resource actor governance storage"
source = "Human request on 2026-07-01: resources need read and update actor lists and row-level security around actors, including organization actors and actor globs."
requirements = [
  "storage.resource-actor-governance",
  "storage.spec-governed-storage-models",
  "storage.spec-governed-migration-intents",
]
text = "Extend persistent resource storage so each stored resource records the actors allowed to read and update it, and Postgres-backed resource storage enforces those actor lists with row-level security policies."
statements = [
  "Generated migration artifacts add read and update actor allow-lists to resource records.",
  "Generated Postgres artifacts include actor matching helpers that support exact actor URIs and glob-style actor URI patterns.",
  "Generated Postgres artifacts enable row-level security policies for resource reads and writes based on the active actor URI set.",
  "Organization actors can appear in resource allow-lists so organization membership can become a storage access boundary.",
]
