# This is a Harness primitive.
# See also: library://meta-harness

name = "repository"
description = "Repository-level knowledge for the Meta Harness repository."
isSystemLibrary = true
read_actors = ["actor://knowledge-agent"]
update_actors = []
agent_excludes = [
  "meta-harness/knowledge-agent/**",
]
