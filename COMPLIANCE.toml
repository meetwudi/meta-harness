# Harness-Compliance: Review applicable compliance with an independent subagent before attesting; if subagent use requires human approval, ask for approval.
# This is a Harness primitive.
# See also: library://meta-harness

[[items]]
id = "meta-harness-compliance"
text = "Verify compliance from library://meta-harness."

[[items]]
id = "memory-primitive-source-of-truth"
text = "Verify application- or domain-specific Memory behavior, including layout, retrieval semantics, write semantics, identifiers, duplicate rules, and Library names, is expressed in the applicable Memory primitive such as `MEMORY.toml` and not hard-coded in generic code or shared Knowledge Agent knowledge."
